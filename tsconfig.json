{
  "compilerOptions": {
    "target": "es2020", //define a versão do js                     
    "module": "commonjs", //módulo padrão do node        
    "rootDir": "./src", //define o diretório de códigos ts                                
    "outDir": "./dist", //define o diretorio de transpilação (código pronto para uso)
    "strict": true, //deixa a validação de tipos de forma estrita
    "esModuleInterop": true, //permite importação padrão de módulos CommomJS                                                            
    "forceConsistentCasingInFileNames": true, //evita erros com maiusculas ou minusculas no import           
  },
  "include": ["src"], //inclui arquivos na pasta src
  "exclude": ["node_modules", "dist"] //exclui essas pastas do processo de transpilação
}
